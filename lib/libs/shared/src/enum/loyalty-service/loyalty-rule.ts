export enum LOYALTY_RULE_MODE_ENUM {
	AUTO = 'automatic',
	WITH_CODE = 'with_a_promotion_code',
}

export enum LOYALTY_RULE_REWARD_POINT_MODE_ENUM {
	ORDER = 'per_order',
	MONEY = 'per_vnd_spent',
	UNIT = 'per_unit_paid',
}

export enum LOYALTY_RULE_TAX_MODE_ENUM {
	INCL = 'included',
	EXCL = 'excluded',
}
