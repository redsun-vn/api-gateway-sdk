export declare namespace IBrandAuth {
    interface IAuthConfig {
        brand: string;
        domains: string[];
        realm: string;
        clientId: string;
        clientSecret: string;
        authServerUrl: string;
    }
}
